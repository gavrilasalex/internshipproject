<mvc:View
    controllerName="intern2020.controller.Login"
    xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m">
<Page title="Login Page" showHeader="true" enableScrolling="false">
	<content >

		<VBox class="containerLogin" justifyContent="Center" alignItems="Center" alignContent="Center">

			<VBox>
				<core:Icon
					src="sap-icon://customer"
					size="6rem"
					class="sapUiMediumMargin"/>
			</VBox>

			<VBox>

				<Input placeholder="E-Mail address" id="emailInput" type="Email" change="validateEmail"/>
				<Input type="Password" id="passwordInput" placeholder="Password"  />
				<Button width="12rem" text="Login" type="Emphasized" press="onLoginPress"></Button>
			</VBox>

		</VBox>
	</content>
	
   <!-- <Table id="idBooksTable"
		items="{
			path: '/Books',
			sorter: {
				path: 'Title'
			}
		}"
		growing="true"
		growingThreshold="10">
	
		<columns>
			<Column>
				<Text text="Title" />
			</Column>
			<Column>
				<Text text="Author" />
			</Column>
			<Column>
				<Text text="Published on" />
			</Column>
			<Column>
				<Text text="Language" />
			</Column>
         <Column>
				<Text text="Available books" />
			</Column>
		</columns>
		<items>
			<ColumnListItem>
				<cells>
					<ObjectIdentifier
						title="{Title}"
						text="{ISBN}"/>
					<Text text="{Author}" />
					<Text text="{ 
							path: 'DatePublished',
							type: 'sap.ui.model.type.Date'
               			}" 
					/>
					<Text text="{Language}" />
					<Text text="{= parseInt(${NubmerAvailableBooks}) }" />
				</cells>
			</ColumnListItem>
		</items>
	</Table> -->
</Page>
</mvc:View>