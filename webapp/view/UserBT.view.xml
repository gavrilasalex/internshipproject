<!-- 
	
User Approved View

Navigation Button -> _onNavBack
Table: Employee, Date, Price, Location
Items (type navigation) -> _onPress 

-->

<mvc:View
    controllerName="intern2020.controller.UserBT"
    xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m"
	xmlns:layout="sap.ui.layout">

<Page   title="My {i18n>businessTrips}" 
        showHeader="true" 
        class="w3-backGround" 
        enableScrolling="false">
	
	<headerContent>
			<Button 
                text="SignOut" 
				class="headerColor"
                press="_onSignOutPress" 
                icon="sap-icon://log">
            </Button>
	</headerContent>

    <Table
        id="UserTable"
        headerText="My Requests"
        class="sapUiResponsiveMargin"
        width="auto">

 		<columns>
			<Column 
				width="20em">
				<Text text="{i18n>employee}"/>
			</Column>
			<Column
				demandPopin="false">
				<Text text="{i18n>date}"/>
			</Column>
			<Column
				demandPopin="false">
				<Text text="{i18n>price}"/>
			</Column>
			<Column
				demandPopin="false">
				<Text text="{i18n>location}"/>
			</Column>
			<Column
				width="20em"
				demandPopin="false">
				<Text text="{i18n>reason}"/>
			</Column>
		</columns>

		<items>
			<ColumnListItem
				type="Navigation"
				press="_onPress">
				<cells>
					<ObjectIdentifier id="employeeName" title="{invoice>name}"/>
					<Text text="{invoice>description}"/>
					<Text text="{invoice>rating}"/>
					<Text text="{invoice>categorie}"/>
					<Text text="{invoice>studio}"/>
				</cells>
			</ColumnListItem>
		</items>
	</Table>
</Page>
</mvc:View>